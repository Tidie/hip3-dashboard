[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Extended timeout for leaderboard function (needs pagination for large accounts)
# Pro plan allows up to 26 seconds
[functions."leaderboard"]
  external_node_modules = ["axios"]
  timeout = 26
